import { UppertablePipe } from './uppertable.pipe';

describe('UppertablePipe', () => {
  it('create an instance', () => {
    const pipe = new UppertablePipe();
    expect(pipe).toBeTruthy();
  });
});
