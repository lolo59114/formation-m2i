import { OrdertablePipe } from './ordertable.pipe';

describe('OrdertablePipe', () => {
  it('create an instance', () => {
    const pipe = new OrdertablePipe();
    expect(pipe).toBeTruthy();
  });
});
